{% extends 'base.html.twig' %}

{% block title %}Ajouter dans le catalogue
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Ajouter dans le catalogue
		</h1>

		{{ form_start(form) }}

		{{ form_row(form) }}

		{{ form_row(form2) }}

		<button class="btn btn-lg btn-primary" type="submit" style="margin-top:10px">
			Ajouter
		</button>


		{{ form_end(form) }}
	</div>
{% endblock %}
